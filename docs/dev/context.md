# Project Context

> Generated by dev-context. Last updated: 2026-09-10
> Confidence: 🔵 = from config/source files, 🟡 = inferred from code, 🔴 = AI assumption

## Tech Stack

| Category | Value | Confidence |
|----------|-------|------------|
| Language | TypeScript 5.8 / JavaScript ESM | 🔵 |
| Framework | Next.js 16, React 19, LangChain.js, LangGraph.js | 🔵 |
| Runtime | Node.js 24 in the current workspace; Cloudflare Workers target | 🔵 |
| Package Manager | pnpm | 🔵 |

## Test Framework

| Item | Value |
|------|-------|
| Framework | Vitest 3 |
| Config File | `vitest.config.ts` |
| Test Command | `pnpm test` |
| Test Directory | `test/` |
| Type Check | `pnpm typecheck` |
| Lint | `pnpm lint` |

## Project Structure

```text
app/                  # Next.js UI and route handlers
lib/                  # LangGraph chat orchestration
scripts/              # Deterministic workspace tooling
test/                 # Vitest contract and tooling tests
.agents/skills/       # Repository-local Codex skills
.codex/               # Repository-local Codex lifecycle hooks
.improvement-loop/    # Anonymized observations and derived rules
```

## Coding Conventions

- 🟡 Use camelCase for values/functions, PascalCase for React components/types, and UPPER_SNAKE_CASE for constants.
- 🔵 Prefer strict TypeScript and ESM imports; `@/*` resolves from the repository root.
- 🟡 Keep validation at system boundaries and return user-safe error messages from route handlers.
- 🔵 Comments describe design intent or constraints rather than narrating obvious operations.
- 🔵 Tests import production contracts rather than maintaining duplicate implementations.

## Key Entry Points

| Module | File | Purpose |
|--------|------|---------|
| Page | `app/page.tsx` | Chat application shell |
| Chat UI | `app/chat.tsx` | Streaming client and session state |
| Chat API | `app/api/chat/route.ts` | Request validation and streaming response |
| Chat graph | `lib/chat-graph.ts` | LangGraph model workflow |
| Improvement CLI | `scripts/improvement-loop/cli.mjs` | Record, verify, promote, and inspect learnings |
| Improvement hooks | `.codex/hooks/improvement-loop.mjs` | Codex lifecycle automation |

## Build & Run

| Command | Description |
|---------|-------------|
| `pnpm dev` | Start the Next.js development server |
| `pnpm build` | Build Next.js |
| `pnpm test` | Run all Vitest tests |
| `pnpm typecheck` | Run TypeScript checks without emitting files |
| `pnpm lint` | Run the configured linter |
| `pnpm cf:build` | Build the Cloudflare target |

## Additional Notes

- 🔵 Local secrets belong in `.env.local`; production secrets use Wrangler secrets.
- 🔵 The improvement loop stores anonymized summaries and identifiers, never raw prompts or transcripts.
- 🔵 No Docker Compose configuration is present.

